const url = "https://webchat-server-aenq.onrender.com";

export const authAPI = `${url}/api/auth`;
export const userAPI = `${url}/api/user`;
export const messageAPI = `${url}/api/message`;
export const baseURL = `${url}`;
